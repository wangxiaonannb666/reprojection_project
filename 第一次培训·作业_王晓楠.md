# 视觉第一周作业报告

## 1. 开发环境、坐标系约定和重投影原理
- **开发环境**：Windows 11 + WSL2 (Ubuntu)，编译器 g++ 15.2.0，CMake 构建工具，VS Code。
- **坐标系约定**：采用右手坐标系，外参约定为世界到相机的变换：Pc = R * Pw + t。
- **重投影原理**：利用理想针孔模型，将三维世界坐标点转换到相机坐标系，再通过内参矩阵投影到像素坐标系。公式：u = fx * (Xc / Zc) + cx; v = fy * (Yc / Zc) + cy。如果 Zc <= 0，则说明点位于相机后方，需抛出异常。

## 2. 项目结构、构建运行方法、测试结果及误差分析
- **项目结构**：
  - include/reprojection.hpp 
  - src/reprojection.cpp 
  - main.cpp 
  - CMakeLists.txt 
- **构建运行**：
  mkdir build && cd build
  cmake ..
  make
  ./reprojection_test
- **测试结果**：
  - 正常点（正深度）投影结果为 (320, 240)，与观测值欧氏距离为 0 像素。
  - 异常点（负深度）成功捕获异常，输出提示“Point is behind the camera (Z <= 0)”。
- **误差分析**：在无畸变理想针孔模型下，理论计算值与观测值一致，误差为0。

## 3. 遇到的问题与解决办法
- **问题1**；下载工具时走了很多弯路，不会写代码
- **问题2**：CMake配置时报错找不到源文件，因为文件名拼写错误
- **问题3**：头文件引用路径混乱导致找不到头文件
- **问题4**：推送到 GitHub 时网络受限，无法连接。解决办法：重新配置远程仓库地址，生成 Personal Access Token 完成推送
- **方法**；寻求AI的帮助，自己多学多琢磨


## 4. 代码仓库链接、PR链接和最终提交的 commit hash
- **仓库链接**：https://github.com/wangxiaonannb666/reprojection_project
- **PR链接**：无（已本地分支合并）
- **Commit Hash**：01c8d14cdf58a80d655f8eea216e9e29fe9d7a2b